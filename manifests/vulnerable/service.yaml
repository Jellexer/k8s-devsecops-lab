apiVersion: v1
kind: Service
metadata:
  name: juiceshop
  namespace: default
spec:
  selector:
    app: juiceshop
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
